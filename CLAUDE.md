# CLAUDE.md

**만들면서 배우는 AI 에이전트 개발 입문+실전** (박나연 / 한빛미디어) 학습 노트 저장소입니다.
이 문서는 다음 세션에서 작업을 이어갈 때 읽는 안내서입니다.

| 문서 | 무엇이 있나 |
| :--- | :--- |
| **CLAUDE.md** (이 문서) | 작업 규칙 · 진행 상태 · 환경 정보 |
| [STUDY.md](STUDY.md) | 목차와 진도표 |
| [history.md](history.md) | **왜 지금 이 모양인지** — 지나온 판단과 **하지 않기로 한 것들** |
| [README.md](README.md) | 저자 원본 README (건드리지 않음) |

이 저장소(`ff-1204/ai-agent`)는 저자의 실습 코드 저장소 **<https://github.com/gongwon-nayeon/hanbit-aiagent>** 를 포크해 학습용으로 재배치한 것입니다.

> **작업을 시작하기 전에 [history.md](history.md)의 "하지 않기로 한 것들"을 한 번 보세요.** 이미 판단이 끝난 것을 다시 꺼내는 일을 막아 줍니다.

---

## 1. 저장소 구조

```
ai-agent/
├── STUDY.md                  # 목차 + 진도표  (K8s 저장소의 README.md 자리)
├── CLAUDE.md                 # 이 문서
├── README.md                 # 저자 원본 README (건드리지 않음)
├── pyproject.toml            # 의존성 — 버전 상한의 근거는 §5
├── ch01_llm-decision/
│   ├── README.md             # 장 개요 · 학습 목표 · 절 색인 · 참고 문서
│   ├── 01-01_….md ~ 01-04_….md   # 절 노트 (파일 하나 = 교재의 한 개 절)
│   ├── notes.md              # 장 전체 요약 (장을 다 쓴 뒤 정리)
│   ├── examples/             # 저자 원본 예제 — 읽기 전용
│   └── practice/             # 직접 쳐보는 공간
└── ch02_… ~ ch11_…           # 같은 구조
```

- **폴더명은 ASCII(`ch06_single-agent`)를 유지합니다.** 한글·공백이 섞인 폴더명은 GitHub에서 상대 경로가 깨집니다. 이 저장소는 그 문제가 없으니 K8s 저장소처럼 `06장. …` 로 바꾸지 마세요.
- `examples/`는 **저자 원본입니다. 수정하지 않습니다.** 노트에서는 경로로만 가리킵니다.
- 각 장 `README.md`의 `## 참고 문서` 표에 그 장에서 볼 공식 문서가 정리돼 있습니다. 노트를 쓸 때 여기서 출발하세요.

## 2. 진행 상태 (2026-08-31 기준)

| 장 | 노트 | 비고 |
| :--- | :--- | :--- |
| **01 ~ 11장 전부** | ✅ **작성 완료** | 48개 절 · mermaid 104개 |
| **표현·이해 흐름** | ✅ **48개 절 전체 적용** | 1~3장 전면 재작성, 4~11장 부분 수정 ([history.md](history.md) ⑧) |

**절 노트 작성은 끝났습니다.** 남은 작업은 아래 넷입니다.

| 할 일 | 상태 | 비고 |
| :--- | :--- | :--- |
| **통독하며 걸리는 표현 찾기** | ⬜ | **다음 차례.** 기준은 세웠지만 학습자 입장에서 읽어 본 적이 없습니다 |
| `question/` 폴더 (장별 면접 질문) | ⬜ | 규칙 미정 — §3 참고 |
| `notes.md` (장 전체 요약) | ⬜ | **직접 공부한 뒤 채우는 칸** |
| 실측 출력 보강 | ⬜ | **이제 가능합니다** — §5. 환경은 갖춰졌고 `.env` 키만 채우면 됩니다 |

> **`notes.md`를 대신 채우지 마세요.** 장 요약과 "막혔던 에러 모음"은 **직접 공부하면서 쓰는 칸**입니다. 절 노트(교재 역할)와 성격이 다릅니다.

> **실측 보강을 할 때.** 환경이 갖춰졌으므로(§5) 4장 이후 절에 실행 결과를 넣을 수 있습니다. 다만 **LLM 응답은 매번 달라지므로** `(실행할 때마다 달라집니다)`를 덧붙이고, 검색 결과처럼 시점에 의존하는 것은 넣지 않는 편이 낫습니다.

### 노트를 고칠 때 지킬 것

* **절 간 상호 참조가 699개** 있습니다. 파일명을 바꾸면 링크가 깨집니다. 바꿔야 한다면 전체 검색으로 함께 고치세요
* 링크는 **URL 인코딩된 상대 경로**입니다(공백은 `%20`). 수동으로 쓰지 말고 기존 링크를 복사해 고치세요
* 각 절은 **앞 절에서 심은 복선을 회수**하는 구조입니다. 한 절만 고치면 앞뒤가 어긋날 수 있습니다

### 앞 장에서 심어 둔 복선 — 뒤 장에서 회수할 것

| 심은 곳 | 내용 | 회수할 곳 |
| :--- | :--- | :--- |
| 1.1 · 2.3 | 컨텍스트 윈도우 → 자르기/요약/검색 | **8장** |
| 2.3 | 체크포인터 = 단기, 스토어 = 장기 | **8.3 · 8.4** |
| 2.4 | for 루프는 멈추면 처음부터 → 그래프가 필요한 이유 | **5장** |
| 2.2 | MCP 도구도 모델 입장에선 똑같음 | **9장** |
| 3.1 | 웹 검색·코딩·RAG 에이전트가 싱글 에이전트 | **6.2 · 6.3 · 6.5** |
| 3.2 | 핸드오프 = `Command` · 네트워크/슈퍼바이저/플래닝 | **7.2 ~ 7.6** |
| 3.3 | 워크플로/자율 축과 싱글/멀티 축의 네 칸 | **11장** |

개념 장을 먼저 쓴 이유는 §5 — 노트를 쓸 당시에는 파이썬 환경이 없어 실습 장에 실측 출력을 넣을 수 없었습니다. **환경은 이후 갖춰졌습니다.**

> **빈 템플릿의 소절 목차는 지우면 복원할 수 없습니다.**
> 아직 안 쓴 절 노트에는 `## 소절 체크` 아래에 교재 **상세 목차의 소절 구성**이 들어 있습니다(예: `6.2.1 [실습] 타빌리 서치 사용법 익히기`). 이건 교재를 봐야만 알 수 있는 정보이고 저장소 어디에도 다른 사본이 없습니다.
> 서술형 노트로 교체할 때 **소절 구성과 번호를 그대로 승계**하세요. 임의로 합치거나 쪼개지 않습니다.

## 3. 노트 작성 규칙

### 독자 — IT 특성화고 학생

**이 저장소의 노트는 IT 특성화고 학생이 읽는다고 가정하고 씁니다.** 표현 수준을 정하는 기준입니다.

| | |
| :--- | :--- |
| **안다고 가정** | 파이썬 기본 문법(`if`/`for`/함수/클래스), JSON, 터미널 기본 명령, 깃 기본, 웹 요청·응답 개념 |
| **모른다고 가정** | 인공신경망·확률통계, API 실무 경험, 벡터·임베딩, 비동기(`async`), OAuth, 컨테이너 |

**지킬 것**

- **처음 나오는 용어는 그 자리에서 한 줄로 풀어 씁니다.** 뒤에서 설명한다고 미루지 않습니다
- **몰라도 되는 것은 몰라도 된다고 명시합니다.** 예: *"트랜스포머 내부 수식을 몰라도 에이전트는 만들 수 있습니다."* 안 그러면 거기서 막혀 진도가 멈춥니다
- **설명보다 코드·예시를 먼저** 보입니다. "안 되는 코드를 먼저 써 보고 왜 안 되는지"가 잘 먹힙니다
- **문장을 짧게** 끊습니다. 한 문장에 개념 하나
- **이미 아는 것에 빗댑니다** — 프롬프트 인젝션↔SQL 인젝션, LLM 라우터↔네트워크 라우터처럼

**하지 말 것**

- **유치하게 쓰지 마세요.** 코드를 짜는 학생입니다. 기술 내용은 정확하게 씁니다
- **쉽게 쓴다고 정확도를 깎지 마세요.** 어려우면 풀어서 길게 쓰되, 틀리게 쓰지 않습니다
- 영어 약어를 설명 없이 던지지 마세요

### 절 사이의 이해 흐름

각 절 노트는 **앞 절이 남긴 문제를 푸는** 구조로 잇습니다. **48개 절 전부에 적용돼 있습니다.** 절을 새로 쓰거나 고칠 때 이 틀을 유지하세요.

맨 앞에 이 세 줄:

```markdown
> **여기까지** — 앞 절에서 알게 된 것
> **이 절의 질문** — 한 문장으로
> **다 읽으면** — 무엇을 할 수 있게 되는지
```

맨 뒤에 `## 다음 절로`:

- **답이 아니라 문제를 던지고 끝냅니다.** 그 문제가 다음 절의 도입부와 이어집니다
- 장의 마지막 절에는 `## N장을 마치며`로 전체를 한 문장으로 묶습니다

장 `README.md`에는 `## 이 장의 이해 흐름`을 두고 **질문이 꼬리를 무는 mermaid**와 절별 질문표를 넣습니다([1장 예시](ch01_llm-decision/README.md)). 11개 장 전부에 있습니다.

> **일괄 편집 뒤에는 검사를 돌리세요.** 48개 파일을 눈으로 다 볼 수 없습니다. 실제로 7.6절에서 도입 블록이 문장 중간에 삽입돼 한 문장이 쪼개진 적이 있습니다. 최소한 이 넷은 확인합니다.
>
> | 검사 | 방법 |
> | :--- | :--- |
> | 도입 블록이 줄 맨 앞인가 | `^> \*\*여기까지\*\*` 로 매칭되는 파일이 48개인가 |
> | 세 줄이 붙어 있는가 | `여기까지` / `이 절의 질문` / `다 읽으면` 줄 번호가 연속인가 |
> | 마무리가 있는가 | `## 다음 절로` + `## N장을 마치며` 합이 48인가 |
> | 푸터가 온전한가 | 마지막 세 줄이 `---` · 빈 줄 · `[⬅ …]` 인가 |
>
> **PowerShell로 돌리세요.** 파일명에 공백과 한글이 있어 Bash의 `for f in …`은 단어 분리로 깨집니다(§7).
>
> **검사 대상에서 `.venv`를 빼세요.** 설치된 패키지에도 `.md`가 들어 있어 링크 검사가 남의 문서를 잡습니다(`onnxruntime/Privacy.md` 등). 제외 패턴: `-notmatch '\\(examples|\.venv)\\'`

### 파일과 제목

- **파일 하나 = 교재의 한 개 절.** 파일명은 `NN-NN_<절 제목>.md` (예: `06-02_웹 검색 에이전트 만들기.md`)
- 문서 제목은 `# <절번호> <절 제목>` (예: `# 6.2 웹 검색 에이전트 만들기`)
- **절·소절 구성은 교재 목차를 따릅니다.** 임의로 쪼개거나 합치지 않습니다
- 환경 구축(4.1 VS Code, 4.2 가상환경, 4.3 환경변수)은 나중에 `SETUP.md`로 뺄 예정입니다. 아직 만들지 않았습니다

### 용어

- **공식 문서 용어를 우선합니다.** 교재 용어가 다르면 **괄호나 대응표로 병기**하고, 본문 서술은 공식 용어로 밀고 나갑니다
- 이유는 검색 가능성입니다. 막혔을 때 찾아볼 곳이 공식 문서이므로, 노트에 적힌 말과 문서에 적힌 말이 같아야 합니다
- 다만 **교재 용어를 지우지는 마세요.** 책을 보면서 노트를 읽을 때 대응이 안 되면 곤란합니다
- 확정된 대응 (2026-08-31)

  | 공식 문서 | 교재 | 비고 |
  | :--- | :--- | :--- |
  | 워크플로(Workflow) | 라우터 기반 에이전트 | 교재의 "라우팅"도 공식 패턴 이름 |
  | 에이전트(Agent) | 자율 에이전트 구조 | |
  | Routing / Orchestrator-worker / Evaluator-optimizer … | — | 워크플로 패턴 5종. [1.4절](ch01_llm-decision/01-04_LLM%20%EA%B8%B0%EB%B0%98%20%EC%97%90%EC%9D%B4%EC%A0%84%ED%8A%B8%20%EC%8B%9C%EC%8A%A4%ED%85%9C%2C%20%EC%9D%B4%EB%9F%B0%20%EA%B5%AC%EC%A1%B0%EB%A1%9C%20%EC%84%A4%EA%B3%84%EB%90%9C%EB%8B%A4.md)에 대응표 |

### 본문

- 개념은 `##`/`###` 소제목 + 불릿으로 전개, 핵심 용어는 **볼드**에 영문 병기 — 예: **도구 호출(Tool Calling)**
- **문제 제기로 시작합니다.** "왜 이게 필요한가"를 먼저 세우고 개념을 꺼냅니다. 정의부터 나열하지 않습니다
- 비교는 표로 정리
- 코드는 언어를 지정한 코드 블록(` ```python `, ` ```powershell `, ` ```json `)
- 함정·주의사항은 `>` 인용 블록으로 강조
- 다른 장으로 이어지는 개념은 `(7장에서 다룹니다)`처럼 명시
- 저자 예제를 인용할 때는 **경로를 함께 적습니다** — 예: `examples/web_agent/agent.py`

### 실행 결과 — 지어내지 않습니다

- 코드 실행 결과, LLM 응답, 에러 메시지는 **실제로 실행해서 얻은 것만** 넣습니다
- 환경이 없어 실행하지 못했으면 **결과 블록을 아예 넣지 않습니다.** 그럴듯한 출력을 만들어 넣는 것이 이 저장소에서 제일 하면 안 되는 일입니다
- LLM 응답은 실행할 때마다 달라집니다. 옮길 때 `(실행할 때마다 달라집니다)`를 덧붙이세요
- API 동작이 불확실하면 **공식 문서로 확인 후** 씁니다. 추측한 파라미터명·버전을 사실처럼 쓰지 않습니다

### 문서 끝 고정 구성

```markdown
## 요약 및 비유
| 개념 | ○○ 비유 | 기술적 의미 |     ← 비유 주제는 문서마다 다르게

## 결론
* 3~5개 불릿으로 핵심 정리

## 공식 문서와 다른 점 (2026년 8월 기준)     ← 실제로 다를 때만
* 교재(집필 시점)와 현재 라이브러리 동작이 달라진 부분
* 참고: <https://docs.langchain.com/...>
```

`공식 문서와 다른 점` 절은 **실제로 다를 때만** 씁니다. 이 교재는 LangChain v1 시기에 쓰였지만 라이브러리가 빠르게 움직이므로, §5의 함정에 걸리는 절에서는 이 절이 특히 중요합니다.

### `question/` 폴더

K8s 저장소처럼 장별 면접 질문 세트를 둘 예정이지만 **아직 규칙을 정하지 않았습니다.** 노트가 어느 정도 쌓인 뒤에 정합니다. 그전에 임의로 만들지 마세요.

## 4. 다이어그램 규칙

### ASCII 아트 금지, mermaid 사용

구조도·흐름도는 **mermaid**로 작성합니다. GitHub이 자동 렌더링하고, 텍스트라 diff 추적이 되며, 한글 폰트 문제가 없습니다.

- 순서·계층: `graph TD` / `graph LR`
- 여러 주체가 주고받는 흐름: `sequenceDiagram` — **에이전트 루프·핸드오프·A2A 통신에 특히 잘 맞습니다**
- 줄바꿈은 `<br/>`, 노드 라벨은 큰따옴표로 감쌉니다

에이전트 학습에서 mermaid가 값어치 있는 지점은 **말로 하면 길어지는 흐름**입니다 — ReAct 루프의 반복, 슈퍼바이저의 위임과 복귀, 미들웨어가 끼어드는 순서, MCP 클라이언트-서버 왕복.

> **mermaid 검증은 아직 자동화하지 않았습니다.** K8s 저장소는 Node.js 컨테이너로 `mermaid.parse()`를 돌립니다. 이 PC에는 Node.js도 podman도 확인되지 않았습니다. 당분간은 GitHub에서 렌더링을 눈으로 확인하세요.

## 5. 실습 환경 — 구축 완료 (2026-08-31)

| | |
| :--- | :--- |
| 패키지 관리 | **uv 0.12.7** — winget 사용자 범위 설치 |
| 파이썬 | **3.12.14** — uv 관리형. `.python-version`에 `3.12` 고정 |
| 가상환경 | `.venv/` — `uv sync`가 만듭니다 |
| 검증 | 교재 예제 import **38종 통과**, 그래프 컴파일·체크포인터 동작 확인 |

```powershell
uv run python scripts/check-env.py
```

`scripts/check-env.py`는 **`examples/` 에서 뽑아낸 import 문을 그대로 시험합니다.** API 키가 없어도 돕니다. **의존성을 건드린 뒤에는 이걸 먼저 돌리세요** — §6의 상한이 풀렸는지 여기서 잡힙니다.

> **관리자 권한이 필요 없습니다.** 회사 노트북이라 계정에 관리자 권한이 없습니다. uv는 `~\.local\bin`(이미 PATH에 있음)과 사용자 프로필에만 씁니다. 시스템 파이썬을 건드리지 않습니다.
>
> **`python`은 여전히 Microsoft Store 스텁입니다.** 이걸 고치지 않았습니다. **`uv run python …` 으로 실행하세요.** `python …` 을 직접 치면 스텁이 잡혀 아무 일도 일어나지 않습니다.

### 실행 방법

```powershell
cd C:\Users\dorim\Study\ai-agent
uv run python ch06_single-agent/practice/my_agent.py
```

```powershell
uv run jupyter lab
```

`uv run`은 **필요하면 `.venv`를 알아서 맞춰 줍니다.** 활성화(`activate`)하지 않아도 됩니다.

패키지를 추가할 때는 `pip install`이 아니라:

```powershell
uv add <패키지>
```

> **`uv add`는 `pyproject.toml`을 고칩니다.** §6의 상한(`mcp<2`·`a2a-sdk<1`)이 걸린 패키지는 건드리지 마세요.

### API 키

장별 `.env`를 **빈 값으로 만들어 뒀습니다.** 값만 채우면 됩니다.

| 장 | 필요한 키 |
| :--- | :--- |
| 04 · 05 | `OPENAI_API_KEY` |
| 06 · 07 · 08 · 09 · 10 | `OPENAI_API_KEY` · `TAVILY_API_KEY` |
| 11 | 위 + `SUPABASE_URL` · `SUPABASE_KEY` + 구글 드라이브 OAuth |

> **`.env`·`credentials.json`·`token.json`은 `.gitignore`에 있습니다.** 확인 명령: `git check-ignore -v <경로>`
>
> **키를 출력하지 마세요.** 노트북 셀 출력은 커밋됩니다. `print(os.environ["OPENAI_API_KEY"])` 같은 코드를 남기면 그대로 올라갑니다.

### 그 외

- `uv.lock`은 **갱신했습니다** (2026-08-31, 상한 안에서 최신). 재현하려면 `uv sync` (`--upgrade` 없이)
- `requirements.txt`는 저자 원본 스냅숏이라 기준이 아닙니다. 필요하면 `uv export`로 다시 뽑습니다
- **이제 실측 출력을 노트에 넣을 수 있습니다.** 다만 §3의 규칙은 그대로 — LLM 응답에는 `(실행할 때마다 달라집니다)`를 덧붙이고, 검색 결과처럼 시점에 의존하는 것은 넣지 않습니다

## 6. 버전 함정 — 문서를 볼 때 반드시 확인

교재 예제가 쓰는 버전과 **각 공식 문서 사이트의 기본 화면이 서로 다릅니다.** 그냥 최신 문서를 보고 따라 치면 어긋납니다. `pyproject.toml`에 상한을 걸어 둔 이유이기도 합니다.

| 대상 | 이 저장소 | 문서 사이트 기본 | 무엇이 깨지나 |
| :--- | :--- | :--- | :--- |
| LangChain / LangGraph | **v1** (`create_agent`) | v1 | 구버전 블로그의 `create_react_agent`(`langgraph.prebuilt`)·`AgentExecutor`·LCEL 체인은 안 돌아감 |
| MCP Python SDK | **1.x** (`from mcp.server.fastmcp import FastMCP`) | **v2** (`from mcp.server import MCPServer`) | 9장 예제 5개 파일. 문서에서 **v1 섹션**을 골라야 함 |
| a2a-sdk | **0.3.x** (프로토콜 스펙 0.3) | **스펙 1.0** | 10·11장 예제 21개 파일. 문서 상단 버전 선택기 확인 |

- `pyproject.toml`의 `mcp>=1.26.0,<2` 와 `a2a-sdk>=0.3.25,<1` 상한을 **풀지 마세요.** 풀면 교재 예제 26개 파일이 한꺼번에 깨집니다
- `langchain`·`langgraph`·`langchain-openai`에도 `<2` 를 걸어 뒀습니다
- 전체 문서 색인: <https://docs.langchain.com/llms.txt> · LangGraph만: <https://docs.langchain.com/oss/python/langgraph/llms.txt>

### OpenAI 문서 도메인이 이전됐습니다 (2026-08-31 확인)

`platform.openai.com/docs/...` → **`developers.openai.com/api/docs/...`** 로 301 리다이렉트됩니다. 노트의 링크는 새 주소로 갱신해 뒀습니다. 새로 링크를 넣을 때도 새 주소를 쓰세요.

**콘솔은 그대로입니다** — API 키 발급 페이지(`platform.openai.com/api-keys`)는 이전되지 않았습니다.

> **모델 이름을 노트에 적지 마세요.** 확인 시점에 모델 문서의 최신 계열이 교재의 `gpt-4o`와 이미 달랐습니다. 이름과 가격은 [모델 문서](https://developers.openai.com/api/docs/models)로 넘기는 것이 맞습니다.

## 7. 작업할 때 유의할 점

- **한국어로 작성합니다.** 기술 용어는 한글 + 영문 병기
- 셸은 **PowerShell**이 기본입니다. Bash 도구도 있지만 문법이 다릅니다
- **대용량 한글 문서를 Bash 히어독으로 쓰지 마세요.** 특수문자에서 깨집니다(이 저장소에서 실제로 겪었습니다). Write 도구를 씁니다
- 파일 경로에 **공백과 한글**이 많습니다. Bash에서 `find ... | xargs` 나 따옴표 없는 변수 전개는 단어 분리로 깨집니다. PowerShell의 `-LiteralPath`나 Glob/Grep 도구를 쓰세요
- 커밋 작성자는 로컬 설정으로 **ff-1204**에 고정돼 있습니다(전역은 회사 계정). 새 저장소에서 작업할 때 주의
- 노트를 고치면 **STUDY.md 진도표**와 해당 장 `README.md`의 절 색인도 함께 맞춥니다

## 8. 참고 링크

- 저자 원본 저장소: <https://github.com/gongwon-nayeon/hanbit-aiagent> (이 저장소의 upstream)
- 교재 상세 페이지: <https://www.hanbit.co.kr/books/B5614034531>
- LangChain / LangGraph 문서: <https://docs.langchain.com/oss/python/langchain/overview>
- MCP: <https://modelcontextprotocol.io/> · Python SDK: <https://py.sdk.modelcontextprotocol.io/>
- A2A: <https://a2a-protocol.org/>
- 같은 방식으로 쓴 선행 저장소: `C:\Users\dorim\Study\Kubernetes-in-Action` — 노트 형식·질문 세트의 기준입니다
